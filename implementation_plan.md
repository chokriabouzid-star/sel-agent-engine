# SEL Agent v7.3.1 — التقرير التقني الشامل وخارطة الطريق (مُحدَّث)

بعد مراجعتي الدقيقة الثانية لكل من حجم الملفات، والتقارير الفعلية (بما فيها تقرير المراجعة النهائي 2026)، ومكونات الكود، **أعتذر عن الاستنتاج السابق وأؤكد ملاحظتك الدقيقة**. 
الملف الذي يعاني من أزمة المركزية (God Object) وأكبر تداخل في المسؤوليات هو **`agent.rs`** وليس `executor.rs`. 

إليك التشخيص الشامل والمُحدّث وخارطة الطريق المستندة إلى المراجعة الحقيقية الدقيقة.

---

## أين نحن الآن وماذا أنجزنا؟ (Current State & Accomplishments)

المشروع في النسخة **v7.3.1** يعمل بثبات ممتاز، وقد تجاوزنا أخطاء المرحلة الأولى (P0 & P1) بنجاح:
✅ **إصلاح الأخطاء المنطقية**: تم حل `BUG-01` (أسبقية المعاملات الخاطئة في التقييم)، وتم تجاوز `BUG-02` (التقييم الخاطئ في نظام الـ Mutation Testing)، وتم إلغاء الكود المثلث المكرر من دالة الـ Patch (BUG-03).
✅ **تنظيف العبء التقني (Dead Code)**: تم التخلص فعلياً من ملف `prompt.rs` بالكامل (1061 سطر)، وتم تفريغ ملف `llm.rs` القديم لصالح `llm_engine.rs`. 
✅ **الثبات**: عملية البناء `cargo check` تمر بنجاح دون أخطاء معرقلة.

---

## ما هي المشاكل الجذرية المتبقية؟ (The Core Architecture Flaws)

برغم النجاحات، النظام يعاني من عنق زجاجة هيكلي في **`agent.rs`** بالإضافة إلى هشاشة واضحة في إدارة السياق والتعديلات، تتلخص في:

> [!CAUTION]
> خطر التعقيد المتمركز (The Real God Object)
> الملف **`agent.rs`** وصل لحجم هائل (أكثر من 1500 سطر - 78 كيلوبايت). هو يُمزج حالياً بين 5 مسؤوليات متعارضة تماماً:
> 1. الـ State Machine (Planning, Executing, Repairing).
> 2. بناء السياق (Context Assembly).
> 3. تجهيز الـ Prompts.
> 4. توجيه التنفيذ (Orchestration).
> 5. منطق الإصلاح (Repair Logic).
> بقاء الملف بهذا الحجم يجعله هشاً أمام أي تحديث مستقبلي (مثل إضافة المشاريع متعددة الملفات).

> [!WARNING]
> تسمم السياق (Context Rot) وهشاشة الـ Patching
> الوكيل يفتقر لمدير سياق ذكي (Context Ledger) مخصص يحمي ميزانية الـ Tokens عند تكرار الفشل. كما أن الإعتماد على الـ Text-search Patching (رغم محاولات فرض قواعد Strict) ما زال عرضة للانهيار بسبب المسافات الفارغة.

---

## خطة العمل المتسلسلة (Action Plan - Detailed Road Map)

بناءً على التقييم، هذه هي المراحل الدقيقة التي يجب أن ننفذها بالترتيب لتحويل النظام من وكيل "ملف واحد" إلى محرك قادر على إدارة "مشاريع كاملة":

### 🔴 المرحلة الأولى (عاجلة): تفكيك المركزية في `agent.rs`
يجب أن نقوم بتقسيم ملف `agent.rs` إلى 4 وحدات مستقلة (بدون تغيير المنطق، فقط إعادة هيكلة):
- `state_machine.rs`: لإدارة انتقال الحالات فقط.
- `context_builder.rs`: لفصل دوال مثل `build_workspace_context` و `build_skeleton_context`.
- `repair_engine.rs`: لتولي إدارة حالات الفشل والتغذية الراجعة.
- `plan_validator.rs`: لمنطق فحص وتأكيد نجاح التعديلات.

### 🟠 المرحلة الثانية: إنشاء الـ Context Ledger
إنشاء إدارة حقيقية للـ Tokens تُراقب استهلاك السياق في الـ State Machine. عند الاقتراب من الحد الأقصى (Budget)، يقوم الـ Ledger بلتخيص مسارات الأخطاء بدلاً من قصّها بعنف، لحماية "وعي" الوكيل.

### 🟡 المرحلة الثالثة: ترقية نظام التعديل (AST / Diff Patching)
التخلص من دالة الاستبدال النصي الحالية وبناء نظام ترقيع يعتمد إما على (Line-Range Patching) أو تكامل مع مكتبة (Tree-sitter) للتعرف الهيكلي على الدوال، وهو ما سيلغي الفشل الدائم بسبب المسافات البادئة (Indentation).

### 🔵 المرحلة الرابعة: ذاكرة المستودع (Workspace & Snapshot)
إضافة `Workspace Abstraction` ليفهم الوكيل روابط الملفات ببعضها، مصحوباً بنظام `Git Stash` آلي (Rollback). هكذا لو كسر الوكيل ملفاً أثناء التعديل، لا ينهار باقي المشروع.

---

## قرار المستخدم (User Approval)

> [!IMPORTANT]
> ملاحظتك كانت صحيحة وفي محلها تماماً؛ `agent.rs` هو العقدة الرئيسية وليس `executor.rs` (الذي يُعتبر محرك أمان قوي برغم كبر حجمه).
> راجع الخطة أعلاه. هل تعطيني الصلاحية الآن لإنشاء الفروع (Modules) الجديدة والبدء في تفكيك وتهذيب **`agent.rs`** كخطوة أولى نحو الاستقرار الكامل؟
