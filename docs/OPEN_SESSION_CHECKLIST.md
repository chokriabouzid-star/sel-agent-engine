# ✅ قائمة فتح الجلسة — Open Session Checklist

## قبل أن تكتب أي شيء للنموذج، افعل هذا أولاً:

### 1) افحص الحالة الحالية
```bash
git status --short
git branch --show-current
git log --oneline -3
cargo check 2>&1 | tail -3
2) اقرأ آخر حالة من السجل
Bash

cat docs/STABILITY_LEDGER.md
3) حدد نوع الجلسة (اختر واحدًا فقط)
 🔧 Engine Fix — إصلاح خطأ في المحرك
 📊 Benchmark Fix — إصلاح حالة اختبار محددة
 ✨ Feature Work — إضافة ميزة جديدة
 🧹 Cleanup — تنظيف وتنظيم
4) حدد هدفًا واحدًا فقط بجملة واحدة
text

الهدف اليوم: _______________________________________________
5) حدد ما هو ممنوع لمسه اليوم
text

ممنوع: ____________________________________________________
6) الصق هذا في بداية المحادثة
انسخ من docs/SESSION_PROMPT.md وأضف عليه:

آخر حالة من STABILITY_LEDGER
الهدف المحدد
الممنوعات
✋ قواعد البداية — لا استثناء
لا ترسل كودًا قبل أن يطلبه النموذج.
لا تطلب حلاً قبل التشخيص.
إذا قفز النموذج للتنفيذ مباشرة، قل:

"توقف. أعطني أوامر قراءة فقط أولاً."
